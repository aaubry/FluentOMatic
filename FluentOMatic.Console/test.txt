syntax Osd
using System.Collections;

.Name("aaa" bb)
/*
.OpenSearchProject(string name
  .Named(String name)
  .Described(String lang, "List<String>" description)*
  .WithUrl(
    .Named(string scheme)
    .Template(string template)
    .Discoverable()?
    .Described(string lang, string description)*
  )*
  .WithQueryUrl(
    .Scheme(string scheme)
    .Template(string template)
    .Discoverable()?
    .Described(string lang, string description)*
  )*
)+
*/